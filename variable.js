const pin=413317;
function pinCode() {
    console.log(pin);
    
}
pinCode();

// Diff between var,let,cont
// Initialization- Not Mandatory for var and let
//                 -mandatory for const

// Update - We can update in var and let
//          - We cant update in const once declared 1 variable

// Re-declaration- allowed in var but not allowed in let and const

// Scope- const- block level cope
//        var  - function level scope 
//        let  -  block level scope

// Hoisting- var can allow hoist 
         